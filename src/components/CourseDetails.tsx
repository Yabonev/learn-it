import React from "react";
import { Paper, Grid } from "@material-ui/core";

const styles = {
  container: {
    flexGrow: 1,
    height: "780px",
    display: "grid",
    alignItems: "center",
    justifyContent: "center"
  }
};

const CourseDetails: React.FC = () => <h3>Test page</h3>;

export default CourseDetails;
